#include <bits/stdc++.h>
#define ll long long

using namespace std;

int main() {
//    ofstream fout("angry.out");
//    ifstream fin("angry.in");
    ofstream fout("../output.txt");
    ifstream fin("../input.txt");

    //Take inputs
    int n, k;
    fin >> n >> k;
    vector<int> cows(n);
    for (int i = 0; i < n; i++){
        fin >> cows[i];
    }

    vector<int> dividers(k);
    for (int i = 0; i < k; i++){

    }

    return 0;
}